'use strict';

const covidWrapper = ({

}) => {
  const covid = async ({

  }) => {
    try {    
      return true;
    } catch (errorCatch) {
      throw errorCatch;
    }
  };
  return {
    covid,
  };

};

module.exports = covidWrapper;
